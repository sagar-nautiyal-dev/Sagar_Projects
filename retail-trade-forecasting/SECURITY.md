# Security and privacy

This project uses aggregated public statistics and does not require credentials, access tokens, personal records, or private configuration.

Do not commit local environment files, credentials, private keys, exported browser data, notebook checkpoints, or documents containing personal identifiers. The repository safety check scans text files and notebooks for common disclosure patterns before changes are shared.
